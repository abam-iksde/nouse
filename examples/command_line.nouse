/* this script is built into the executable and is called over and over if you run it
without specifying a file in command line/terminal.
side effect of such solution is that name 'f' cannot be used for user's variable.
also, calling 'f()' in command line will result in an endless recursive loop.*/
print("> ");
let f = function_from_string(input(),"command_line");
f@@();
